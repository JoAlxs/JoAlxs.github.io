{
  "name": "JoAlxs.Github.io",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "deploy": "npm run build && npx gh-pages -d dist"
  },
  "dependencies": {
    "astro": "^5.6.1",
    "pocketbase": "^0.26.0"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "tailwindcss": "^4.1.4"
  }
}
